---
import { siteConfig } from '../config/site';
import { MEDIA_BASE_URL } from '../config/media';

export interface Props {
  contactImageSrc?: string;
}

// Helper function to get the full media URL
function getMediaUrl(src: string): string {
	// If src already starts with http or is a full URL, return as-is
	if (src.startsWith('http') || src.startsWith('//')) {
		return src;
	}
	// If src starts with /astro-portfolio/assets, replace with MEDIA_BASE_URL
	if (src.startsWith('/astro-portfolio/assets')) {
		return src.replace('/astro-portfolio/assets', MEDIA_BASE_URL);
	}
	// If src starts with /assets, handle based on mode
	if (src.startsWith('/assets')) {
		// In dev mode, MEDIA_BASE_URL is /assets, so return path as-is
		if (MEDIA_BASE_URL === '/assets') {
			return src;
		}
		// In prod mode, replace /assets with GCS base URL
		return src.replace('/assets', MEDIA_BASE_URL);
	}
	// Otherwise, prepend MEDIA_BASE_URL
	return `${MEDIA_BASE_URL}${src.startsWith('/') ? '' : '/'}${src}`;
}

// Default contact image if not provided
const defaultImageSrc = getMediaUrl('/assets/site/251009_MakerCamp28839_SOCIAL.jpeg');
const imageSrc = Astro.props.contactImageSrc || defaultImageSrc;
---

<section id="contact" class="contact-section">
	<div class="contact-header">
		<a href="/projects/aboutme" class="contact-image-link">
			<img 
				src={imageSrc}
				alt="Maker Camp" 
				class="contact-image"
			/>
		</a>
		<div class="contact-text">
			<h2>Reach Out!</h2>
			<p class="contact-description">
				Interested in collaborating, hiring, or just want to chat about a project? 
				I'm always excited to connect with fellow makers and creators. Learn more about me
				<a href="/projects/aboutme">here</a>.
			</p>
		</div>
	</div>
	<div class="contact-links">
		<a href={`mailto:${siteConfig.contact.email}`} class="contact-link email">
			<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
				<path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
				<polyline points="22,6 12,13 2,6"/>
			</svg>
			{siteConfig.contact.email}
		</a>
		<a href={siteConfig.contact.github} class="contact-link github" target="_blank">
			<svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
				<path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
			</svg>
			GitHub
		</a>
		<a href={siteConfig.contact.instagram} class="contact-link instagram" target="_blank">
			<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
				<rect x="2" y="2" width="20" height="20" rx="5" ry="5"/>
				<path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"/>
				<line x1="17.5" y1="6.5" x2="17.51" y2="6.5"/>
			</svg>
			Instagram
		</a>
		<a href={siteConfig.contact.linkedin} class="contact-link linkedin" target="_blank">
			<svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
				<path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
			</svg>
			LinkedIn
		</a>
	</div>
</section>

<style>
	.contact-section {
		margin-bottom: 4rem;
		padding: 3rem;
		background: #1a1a1a;
		border-radius: 1rem;
	}

	.contact-header {
		display: flex;
		align-items: flex-start;
		gap: 2rem;
		padding: 0 4rem;
		margin-bottom: 2rem;
	}

	.contact-image-link {
		flex-shrink: 0;
		display: inline-block;
		transition: transform 0.2s ease, box-shadow 0.2s ease;
		text-decoration: none;
		border-radius: 0.5rem;
	}

	.contact-image-link:hover {
		transform: translateY(-4px);
		box-shadow: 0 8px 16px rgba(0, 0, 0, 0.4);
	}

	.contact-image {
		width: 120px;
		height: auto;
		border-radius: 0.5rem;
		object-fit: cover;
		display: block;
	}

	.contact-text {
		flex: 1;
		padding: 0 2rem;
		text-align: left;
	}

	.contact-section h2 {
		font-size: 2.5rem;
		font-weight: 700;
		margin-bottom: 1rem;
		text-align: left;
	}

	.contact-description {
		font-size: 1.1rem;
		color: #cccccc;
		margin-bottom: 0;
	}

	.contact-links {
		display: flex;
		justify-content: center;
		gap: 2rem;
		flex-wrap: wrap;
	}

	.contact-link {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		padding: 1rem 1.5rem;
		background: #2a2a2a;
		border-radius: 0.5rem;
		color: #ffffff;
		text-decoration: none;
		font-weight: 500;
		transition: all 0.2s;
		border: 1px solid #333;
	}

	.contact-link:hover {
		background: var(--accent-color, #3b82f6);
		transform: translateY(-2px);
	}

	.contact-link svg {
		width: 20px;
		height: 20px;
	}

	@media (max-width: 768px) {
		.contact-section {
			padding: 2rem 1.5rem;
		}

		.contact-header {
			flex-direction: column;
			align-items: center;
			text-align: center;
			padding: 0;
		}

		.contact-text {
			text-align: center;
			padding: 0;
		}

		.contact-section h2 {
			text-align: center;
		}

		.contact-links {
			flex-direction: column;
			align-items: center;
		}

		.contact-link {
			width: 100%;
			max-width: 300px;
			justify-content: center;
		}
	}
</style>

